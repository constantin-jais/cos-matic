/target
**/*.rs.bk
Cargo.lock.bak
.DS_Store

# Local Claude Code overrides (machine-specific, never committed)
.claude/settings.local.json

# Audit trail carries timestamps (non-reproducible); the lockfile is committed.
**/.harness/audit.jsonl
